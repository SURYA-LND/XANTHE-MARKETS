import React from 'react';
import { BotMessageSquare, Mail, CalendarCheck, TrendingUp } from 'lucide-react';
import AnimatedSection from './ui/AnimatedSection';

const features = [
  {
    icon: <BotMessageSquare className="h-8 w-8 text-brand-blue" />,
    title: 'Natural Language Prompts',
    description: 'Simply describe the news you want. Our AI understands and fetches the most relevant information for you.',
  },
  {
    icon: <TrendingUp className="h-8 w-8 text-brand-blue" />,
    title: 'Multi-Asset Coverage',
    description: 'Stay on top of Forex, Cryptocurrencies, and Stock markets from a single, unified feed.',
  },
  {
    icon: <Mail className="h-8 w-8 text-brand-blue" />,
    title: 'Email Delivery',
    description: 'Get your personalized news digest delivered straight to your inbox, ready for your morning routine.',
  },
  {
    icon: <CalendarCheck className="h-8 w-8 text-brand-blue" />,
    title: 'Scheduled Delivery',
    description: 'Configure your news to arrive exactly when you need it, every day at your preferred time.',
  },
];

const Features: React.FC = () => {
  return (
    <div className="py-24 bg-brand-dark sm:py-32">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <AnimatedSection className="max-w-2xl mx-auto lg:text-center">
          <h2 className="text-base font-semibold leading-7 text-brand-blue">Your Personal Analyst</h2>
          <p className="mt-2 text-3xl font-bold tracking-tight text-white sm:text-4xl">
            Everything you need for market intelligence
          </p>
          <p className="mt-6 text-lg leading-8 text-brand-gray-light">
            MarketPulse.AI is more than just a news aggregator. It's a smart agent that works for you.
          </p>
        </AnimatedSection>
        <div className="mt-16 max-w-2xl mx-auto sm:mt-20 lg:mt-24 lg:max-w-none">
          <dl className="grid max-w-xl grid-cols-1 gap-x-8 gap-y-16 lg:max-w-none lg:grid-cols-2">
            {features.map((feature, index) => (
              <AnimatedSection key={feature.title} delay={0.2 * (index % 2)}>
                <div className="flex">
                  <div className="flex h-12 w-12 items-center justify-center rounded-lg bg-brand-blue/10">
                    {feature.icon}
                  </div>
                  <div className="ml-5">
                    <dt className="text-xl font-semibold leading-7 text-white">{feature.title}</dt>
                    <dd className="mt-1 text-base leading-7 text-brand-gray-light">{feature.description}</dd>
                  </div>
                </div>
              </AnimatedSection>
            ))}
          </dl>
        </div>
      </div>
    </div>
  );
};

export default Features;
