import React from 'react';
import { PencilLine, Link, Send } from 'lucide-react';
import AnimatedSection from './ui/AnimatedSection';

const steps = [
  {
    name: 'Step 1: Describe',
    description: 'Tell our AI what financial news you\'re interested in. Be as specific or broad as you like.',
    icon: PencilLine,
  },
  {
    name: 'Step 2: Configure',
    description: 'Set your delivery schedule and connect your email or webhook for seamless integration.',
    icon: Link,
  },
  {
    name: 'Step 3: Receive',
    description: 'Get your tailored market insights on time, every time. Focus on strategy, not on searching.',
    icon: Send,
  },
];

const HowItWorks: React.FC = () => {
  return (
    <div className="py-24 sm:py-32 bg-brand-gray/20">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <AnimatedSection className="max-w-2xl mx-auto text-center">
          <h2 className="text-3xl font-bold tracking-tight text-white sm:text-4xl">Get your news in 3 simple steps</h2>
          <p className="mt-4 text-lg text-brand-gray-light">
            From prompt to inbox, the process is designed to be effortless.
          </p>
        </AnimatedSection>
        <div className="relative mt-16">
          <div aria-hidden="true" className="absolute inset-x-0 top-1/2 -translate-y-1/2 h-0.5 w-full bg-brand-gray-dark/50 hidden md:block"></div>
          <div className="relative grid grid-cols-1 md:grid-cols-3 gap-12">
            {steps.map((step, index) => (
              <AnimatedSection key={step.name} delay={index * 0.2}>
                <div className="relative p-8 bg-brand-gray rounded-xl border border-brand-gray-dark text-center">
                  <div className="flex items-center justify-center h-16 w-16 rounded-full bg-brand-blue/10 border-2 border-brand-blue/30 mx-auto mb-6">
                    <step.icon className="h-8 w-8 text-brand-blue" />
                  </div>
                  <h3 className="text-xl font-semibold text-white">{step.name}</h3>
                  <p className="mt-2 text-brand-gray-light">{step.description}</p>
                </div>
              </AnimatedSection>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
};

export default HowItWorks;
