import React from 'react';
import { CheckCircle2, Zap } from 'lucide-react';
import AnimatedSection from './ui/AnimatedSection';

const includedFeatures = [
  'Unlimited AI Agent Prompts',
  'Daily Email Delivery',
  'Multi-Asset News Coverage',
  'Priority Support',
  'All Future Updates',
];

const Pricing: React.FC = () => {
  return (
    <div className="py-24 sm:py-32">
      <div className="container mx-auto px-4 sm:px-6 lg:px-8">
        <AnimatedSection className="mx-auto max-w-2xl sm:text-center">
          <h2 className="text-3xl font-bold tracking-tight text-white sm:text-4xl">One-time payment, lifetime access.</h2>
          <p className="mt-6 text-lg leading-8 text-brand-gray-light">
            Unlock the full potential of MarketPulse.AI with a single payment. No subscriptions, no hidden fees.
          </p>
        </AnimatedSection>
        <AnimatedSection delay={0.2}>
          <div className="mx-auto mt-16 max-w-lg rounded-3xl ring-2 ring-brand-blue bg-brand-gray/30 p-8 sm:p-10">
            <h3 className="text-2xl font-bold tracking-tight text-white">Lifetime Membership</h3>
            <div className="mt-4 flex items-baseline gap-x-2">
              <span className="text-5xl font-bold tracking-tight text-white">$19</span>
              <span className="text-base font-semibold leading-7 text-brand-gray-light">USD</span>
            </div>
            <p className="mt-6 text-base leading-7 text-brand-gray-light">
              Get lifetime access to our AI-powered news agent and all its features.
            </p>
            <a
              href="#"
              className="mt-10 flex w-full items-center justify-center rounded-md bg-brand-blue px-3.5 py-2.5 text-center text-lg font-semibold text-white shadow-sm hover:bg-blue-600 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-blue transition-colors duration-300 group"
            >
              Get Access Now <Zap className="ml-2 h-5 w-5 group-hover:animate-ping" />
            </a>
            <ul role="list" className="mt-8 space-y-3 text-sm leading-6 text-brand-gray-light">
              {includedFeatures.map((feature) => (
                <li key={feature} className="flex gap-x-3">
                  <CheckCircle2 className="h-6 w-5 flex-none text-brand-blue" aria-hidden="true" />
                  {feature}
                </li>
              ))}
            </ul>
          </div>
        </AnimatedSection>
      </div>
    </div>
  );
};

export default Pricing;
